# Local dev profile — copy to deploy/.env when using `docker compose` directly
# (the Makefile passes these via `make up-local`).

AGENTCOOPDB_INSECURE_HTTP=true
AGENTCOOPDB_LOG_FORMAT=text
AGENTCOOPDB_LOG_LEVEL=debug
